pipeline {
  agent {
    label 'jenkins-agent-npm'
  }

  stages {
    stage ('Install Dependencies') {
      steps {
        sh """
					cd exchange-cli
					npm install
				"""
      }
    }

    stage ('Build') {
      steps {
        sh """
          cd exchange-cli
          npm run build
        """
      }
    }
  }
}
