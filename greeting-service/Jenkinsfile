pipeline{
    agent any

    stages{
        stage('Deploy') {
            steps {
                sh '''
                    oc project btutpi-deploy-strategies
                    oc start-build greeting-service --follow --wait
                '''
            }
        }
    }
}
